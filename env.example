# Database (สำหรับ Development - SQLite)
DATABASE_URL="file:./dev.db"

# Database (สำหรับ Production - Supabase PostgreSQL)
# DATABASE_URL="postgresql://username:password@host:port/database?schema=public"

# NextAuth.js
NEXTAUTH_URL="http://localhost:3000"  # สำหรับ Development
# NEXTAUTH_URL="https://your-app-name.vercel.app"  # สำหรับ Production
NEXTAUTH_SECRET="your-nextauth-secret-here"  # สร้างด้วย: openssl rand -base64 32

# Google OAuth (ต้องไปสร้างที่ Google Console)
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"
